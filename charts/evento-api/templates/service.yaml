apiVersion: v1
kind: Service
metadata:
  name: {{ template "evento-api.fullname" . }}
  labels:
    app: {{ template "evento-api.name" . }}
    chart: {{ template "evento-api.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "evento-api.name" . }}
    release: {{ .Release.Name }}
